#include <iostream>
using namespace std;

int main() 
{
    int a, b;
    cout<<"enter two values:";
    cin >> a >> b;
    if(a > b)
        cout <<"the largest value is "<<a;
    else
        cout <<"the largest value is "<<b;
    return 0;
}
